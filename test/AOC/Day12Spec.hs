module AOC.Day12Spec (spec) where

import AOC (day12, parse, part1, part2)
import Test.Hspec

spec :: Spec
spec = do
    describe "day n ..." $ do
        it "no-op" $ do
            () `shouldBe` ()
