module AOC.Day24Spec (spec) where

import AOC (day24, parse, part1, part2)
import Test.Hspec

spec :: Spec
spec = do
    describe "day n ..." $ do
        it "no-op" $ do
            () `shouldBe` ()
