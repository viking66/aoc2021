module AOC.Day25Spec (spec) where

import AOC (day25, parse, part1, part2)
import Test.Hspec

spec :: Spec
spec = do
    describe "day n ..." $ do
        it "no-op" $ do
            () `shouldBe` ()
